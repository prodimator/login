** Building and running python-api with Docker **
- cd into python-api directory
- run 'docker build -t python-api:latest .'
- run 'docker run -d --name python-api -p 5000:5000 python-api:latest'
- make sure to docker kill after you are done.

Alternatively you can run without Docker
- make sure you are using python3
- cd into python-api
- run 'pip install -r requirements.txt'
- run 'python app.py'

** Building and running login-page with Docker **
- cd into login-page
- run 'docker build -t login-app:latest .'
- run 'docker run -d --name login-app -p 3000:3000 login-app:latest'
- kill docker process after you are done

Alternatively, without Docker
-npm install
-npm start